{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-empty": [2, "never"],
    "scope-enum": [2, "always", [
      "client",
      "deps",
      "netlify",
      "server",
      "typescript",
      "webpack",
      "react",
      "server"
    ]]
  }
}
