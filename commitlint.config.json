{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-empty": [2, "never"],
    "scope-enum": [
      2,
      "always",
      ["deps", "docker", "github", "netlify", "typescript", "webpack", "react"]
    ]
  }
}
